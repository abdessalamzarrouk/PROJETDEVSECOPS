package com.main.projetstage.repositories;

import com.main.projetstage.models.PosteComptable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PosteComptableRepository extends JpaRepository<PosteComptable, Long> {
}
